steps:
  # Step 1: Build Docker image
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t', 'REGION-docker.pkg.dev/prox-api-467715/REPO-NAME/IMAGE-NAME:$COMMIT_SHA',
      '.'
    ]

  # Step 2: Push Docker image to Artifact Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'push',
      'REGION-docker.pkg.dev/prox-api-467715/REPO-NAME/IMAGE-NAME:$COMMIT_SHA'
    ]

  # (Optional) Deploy to Cloud Run or GKE - can be added later

images:
  - 'REGION-docker.pkg.dev/prox-api-467715/REPO-NAME/IMAGE-NAME:$COMMIT_SHA'
  
